version: 1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    build:
      commands: []
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - "index.html"
      - "app.html"
      - "code.html"
      - "architecture.html"
      - "/assets/**"
  cache:
    paths: []
